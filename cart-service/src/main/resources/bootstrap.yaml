spring:
  application:
    name: cart-service # 服务名称
  cloud:
    nacos:
      server-addr: 192.168.182.100:8848 # 服务的注册
      config:
        file-extension: yaml # 文件名扩展
        shared-configs:
          - data-id : sky-seata.yaml